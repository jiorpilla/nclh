{% extends 'base.html.twig' %}

{% block title %}Branch index{% endblock %}

{% block contents %}
    <h1>Branch index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Id</th>
                <th>Deleted</th>
                <th>Created_by</th>
                <th>Updated_by</th>
                <th>Created_at</th>
                <th>Updated_at</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for branch in branches %}
            <tr>
                <td>{{ branch.name }}</td>
                <td>{{ branch.id }}</td>
                <td>{{ branch.deleted }}</td>
                <td>{{ branch.createdBy }}</td>
                <td>{{ branch.updatedBy }}</td>
                <td>{{ branch.createdAt ? branch.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ branch.updatedAt ? branch.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_branch_show', {'id': branch.id}) }}">show</a>
                    <a href="{{ path('app_branch_edit', {'id': branch.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_branch_new') }}">Create new</a>
{% endblock %}
