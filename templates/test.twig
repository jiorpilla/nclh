{% extends 'base.html.twig' %}

{%  block body %}
    <div {{ stimulus_controller('say-hello') }}>
        <input type="text" {{ stimulus_target('say-hello', 'name') }}>

        <button {{ stimulus_action('say-hello', 'greet') }}>
            Greet
        </button>

        <div {{ stimulus_target('say-hello', 'output') }}></div>
    </div>
    <div {{ stimulus_controller('hello') }}></div>



    {#    {{ dump(_context) }}#}
    <br>
    Whole URL{{ app.request.uri }}
    <br>
    this will only return path and no GET params{{ app.request.pathinfo }}
    <br>
    return path with GET PARAMS{{ app.request.requesturi }}
{%  endblock %}