{% extends 'base.html.twig' %}

{%  block body %}
<div>
    <a href="{{ qr_code_url('000001', 'pdf') }}" download>DOWNLOAD PDF</a>
</div>
<div>
    <a href="{{ qr_code_url('000001') }}" download>DOWNLOAD IMAGE</a>
</div>
    <img src="{{ qr_code_path('000001') }}" />
    <img src="{{ qr_code_url('000001') }}" />
    <img src="{{ qr_code_data_uri('000001') }}" />

{%  endblock %}