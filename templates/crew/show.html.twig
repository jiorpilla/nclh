{% extends 'base.html.twig' %}

{% block title %}Crew{% endblock %}

{% block contents %}
    <h1>Crew</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_number</th>
                <td>

{#                    <img src="{{ qr_code_path(crew.idNumber) }}" />#}
{#                    <img src="{{ qr_code_url(crew.idNumber) }}" />#}
                    <img src="{{ qr_code_data_uri(crew.id) }}" />


                    <div>
                        <a href="{{ qr_code_url(crew.idNumber, 'pdf') }}" download>DOWNLOAD PDF</a>
                    </div>
                    <div>
                        <a href="{{ qr_code_url(crew.idNumber) }}" download>DOWNLOAD IMAGE</a>
                    </div>
                </td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ crew.position }}</td>
            </tr>
            <tr>
                <th>Ship</th>
                <td>{{ crew.ship }}</td>
            </tr>
            <tr>
                <th>Nationality</th>
                <td>{{ crew.nationality }}</td>
            </tr>
            <tr>
                <th>Passport_number</th>
                <td>{{ crew.passportNumber }}</td>
            </tr>
            <tr>
                <th>Seaman_book_number</th>
                <td>{{ crew.seamanBookNumber }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ crew.company }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ crew.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ crew.lastName }}</td>
            </tr>
            <tr>
                <th>Middle_name</th>
                <td>{{ crew.middleName }}</td>
            </tr>
            <tr>
                <th>Suffix</th>
                <td>{{ crew.suffix }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ crew.gender }}</td>
            </tr>
            <tr>
                <th>Date_of_birth</th>
                <td>{{ crew.dateOfBirth ? crew.dateOfBirth|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Location_of_birth</th>
                <td>{{ crew.locationOfBirth }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ crew.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ crew.email }}</td>
            </tr>
            <tr>
                <th>Civil_status</th>
                <td>{{ crew.civilStatus }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ crew.picture }}</td>
            </tr>
            <tr>
                <th>Created_by</th>
                <td>{{ crew.createdBy }}</td>
            </tr>
            <tr>
                <th>Updated_by</th>
                <td>{{ crew.updatedBy }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ crew.createdAt ? crew.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ crew.updatedAt ? crew.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crew_index') }}">back to list</a>

    <a href="{{ path('app_crew_edit', {'id': crew.id}) }}">edit</a>

    {{ include('crew/_delete_form.html.twig') }}
{% endblock %}
